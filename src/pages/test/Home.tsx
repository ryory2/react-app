import React from 'react';

//TypeScriptでは、コンポーネントの型を定義するためにReact.FC（Functional Component）を使用します。
//React.FCは、関数コンポーネントが受け取るプロパティの型を指定しますが、ここではプロパティがないため、単にReact.FCとしています。
export const Home: React.FC = () => {
    return (
        <div>
            <h1>Home2</h1>
        </div>
    );
}